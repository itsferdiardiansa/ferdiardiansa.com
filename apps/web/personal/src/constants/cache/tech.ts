import type { Cache } from '.'

/**
 * A cache control for about tech skills
 *
 * @constant
 */
export const TECH_SKILLS: Cache = {
  CACHE_TAGS: ['home:skills'],
  CACHE_PATH: '/',
  CACHE_KEY: 'tech-skills',
  TTL: 3600,
}
